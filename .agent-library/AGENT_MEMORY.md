# Agent Memory

This repository is a reusable agent workflow kit.

Keep project-specific operational facts, private infrastructure details, and sensitive runtime notes out of the public repository. Put deployment-specific memory in the consuming project's private agent memory file instead.
